
import { Reaction } from "/server/api";

Reaction.registerPackage({
  label: "Catalog",
  name: "reaction-catalog",
  icon: "fa fa-book",
  autoEnable: true,
  settings: {
    name: "Catalog"
  }
});
