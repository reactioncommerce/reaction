export default async function handleCartPromotionsAnalysisComplete(context, { cart, qualifiedPromotions }) {
  // console.log("qualifiedPromotions", JSON.stringify(qualifiedPromotions, null, 4));
  // remove any offer promotions that no longer qualify
}
