/* eslint-disable no-unused-vars */

/**
 * @summary Get the group discount total for a order
 * @param {Object} context - The application context
 * @param {Object} params.commonOrder - The order to get the group discount total for
 * @returns {Number} The total discount amount for the order
 */
export default function getGroupDiscountTotal(context, { commonOrder }) {
  return 0;
}
