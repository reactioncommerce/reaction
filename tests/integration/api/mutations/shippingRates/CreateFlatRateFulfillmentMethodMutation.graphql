mutation($input: CreateFlatRateFulfillmentMethodInput!) {
  createFlatRateFulfillmentMethod(input: $input) {
      method {
        cost
        fulfillmentTypes
        group
        handling
        isEnabled
        label
        name
        rate
      }
    }
}
