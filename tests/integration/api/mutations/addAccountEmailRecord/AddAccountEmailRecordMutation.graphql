mutation ($accountId: ID!, $email: Email!) {
  addAccountEmailRecord(input: { accountId: $accountId, email: $email }) {
    account {
      emailRecords {
        address
        provides
        verified
      }
    }
  }
}
