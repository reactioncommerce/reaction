mutation($input: RemoveAccountEmailRecordInput!) {
  removeAccountEmailRecord(input: $input) {
      account {
        _id
        name
        emailRecords {
          address
        }
      }
    }
}
