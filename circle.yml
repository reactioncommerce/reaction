machine:
  node:
    version: 0.10.36

dependencies:
  cache_directories:
    - "~/.meteor"
  pre:
    - meteor || curl https://install.meteor.com | /bin/sh
    - cd $HOME/$CIRCLE_PROJECT_REPONAME/bin && ./clone-packages.sh
    - cd $HOME/$CIRCLE_PROJECT_REPONAME && meteor update

test:
  override:
    - cd $HOME/$CIRCLE_PROJECT_REPONAME && JASMINE_CLIENT_UNIT=0 JASMINE_CLIENT_INTEGRATION=0 meteor --test --once
